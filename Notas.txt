

# BBDD impacto cambios 
$ python manage.py makemigrations denuncia
$ python manage.py migrate denuncia

# Django, crear administrador
$ python manage.py createsuperuser



# Uso Git
# Commit local
1) $ git status 
2) $ git add --all .
3) $ git commit -m “ingresar comentario”

# Commit en Github
$ git push -u origin master

# Subir a Server PythonAnywhere (Desde una consola bash)
1) $ git fetch
2) $ git pull

# Si se quiere una copia limpia en local:
1) $ git reset --hard
2) $ git pull

